import { Grid, styled } from "@nextui-org/react";

export const StyledSkillsContainer =  styled(Grid.Container, {
    margin: 'auto'
})

export const StyledSkills = styled(Grid, {
    marginBottom: '$10',
    width: '100%'
})